
// Re-export the supabase client from the integrations folder
// This ensures we use the properly configured client with the correct credentials
export { supabase } from '@/integrations/supabase/client'
